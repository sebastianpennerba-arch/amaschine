/* ----------------------------------------------------------
   CAMPAIGNS – index.js
   Bindet compute + render zusammen
-----------------------------------------------------------*/

import { render } from "./render.js";

export { render };
